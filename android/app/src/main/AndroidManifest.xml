<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ════════════ PERMISSIONS ════════════ -->
    <!-- Voice conversation with Mayra (runtime, dangerous) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Gemini API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- "Call Rahul" contact lookup (runtime, dangerous) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Background notifications — Android 13+ (runtime, dangerous) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Smoother audio queue (normal, optional) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Location awareness (runtime, dangerous) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ════════════ HARDWARE FEATURES ════════════ -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Accessibility service — presence makes the app appear in the
             system Accessibility list so the user can enable it. -->
        <service
            android:name=".MayraAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- ════════════ APP VISIBILITY (Android 11+) ════════════ -->
    <queries>
        <!-- WhatsApp -->
        <package android:name="com.whatsapp" />
        <!-- YouTube -->
        <package android:name="com.google.android.youtube" />
        <!-- Instagram -->
        <package android:name="com.instagram.android" />
        <!-- Chrome -->
        <package android:name="com.android.chrome" />
        <!-- Spotify -->
        <package android:name="com.spotify.music" />
        <!-- Phone/Dialer intent -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <!-- General URL intent -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

</manifest>
